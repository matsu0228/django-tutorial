from django.conf.urls import url
from django.contrib import admin
from django.contrib.auth.views import login, logout_then_login

urlpatterns = [
  # url(r'^alert_setting/$', alert_setting, name='alert_setting')
]
